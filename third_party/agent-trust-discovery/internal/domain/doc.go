// Package domain holds the pure types of the agent-trust-discovery reference
// implementation: agents, signals, the five Trust Vector dimensions, and the
// Trust Evaluation payload. It has no infrastructure dependencies and no JSON
// tags — wire shapes live in DTO packages (design §3).
package domain
