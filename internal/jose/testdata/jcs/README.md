RFC 8785 (JCS) sample vectors, copied unchanged from github.com/gowebpki/jcs
v1.0.1 testdata (Apache-2.0), which carries the RFC author's reference test
data. values.json is the example in RFC 8785 section 3.2.2.
