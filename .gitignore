.DS_Store
bin/
certs/
*.key
*.pem
.env
*.db
.run/
/agent
# Stray binaries if a `go build ./cmd/<x>/` is run from the repo root.
/battery
/tiercheck
/opsflow
/passes
/satreg
/cardhash
/trustseed
# Non-secret prod values (Agent IDs, ports) but per-deployment; keep untracked.
deploy/agents.env
